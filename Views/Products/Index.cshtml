@using WebApplication7.Data.Models
@model IList<Product>

<div class="row g-3">
    @foreach (Product product in Model)
    {
        <partial name="Components/ProductCardPartial" model="product" />
    }
</div>